package dsa_practise_arrays_11th_May_2022;

public class JitendraCode {

	public static void main(String str) {
		/*
		 * String temp = "hello"; String rev = "";
		 * 
		 * int j = 0; for (int i = str.length() - 1; i >= 0; i--) {
		 * 
		 * if (!Character.isLetter(str.charAt(i))) rev += str.charAt(i);
		 * 
		 * } System.out.println("reverse  " + rev); for (int i = 0; i < str.length();
		 * i++) { char ch = str.charAt(i); if (Character.isLetter(ch)) { temp = temp +
		 * rev.charAt(j); j++; } else { temp = temp + ch; } } System.out.println(temp);
		 * }
		 */
	}
}
